class Player:

    tagged = False
    position = {'x': 0, 'y': 0}


    def __init__(self, x, y, it):
        self.x = x
        self.y = y 
        self.it = it

    def move(self, x, y):


        if (not self.moving):
            # choose new position
            # magicalML(board, self)
      
        
